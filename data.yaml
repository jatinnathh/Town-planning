# Absolute path to the root folder
path: C:/Users/Jatin/Desktop/Town Planning/yolo_data2/yolo_data

# Relative path from 'path' to the images
train: images/train
val: images/train  # We use the same set for validation since you are prototyping

# CLASSES: UPDATE THIS based on what you see in Step 1
# Example: if you saw ID:0 on a Bead Factory, keep 0 as bead_factory
names:
  0: warehouse
  1: reservoir
  2: loading_platform
  3: dockyard
  4: citadel
  5: big_house
  6: bead_factory
  # Add more lines if you have more classes